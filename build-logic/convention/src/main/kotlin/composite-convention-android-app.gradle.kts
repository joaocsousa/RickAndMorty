/**
 * Convention plugin for Kotlin Multiplatform libraries.
 *
 * Note: Consuming modules must specify `android.namespace`.
 */
plugins {
    id("foundation-convention-android-app")
    id("foundation-convention-ktlint")
}
